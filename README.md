# Reboot Launcher

Greenfield rewrite. This branch starts from an empty tree and grows one component at a time.

- `web-frontend/`: the rebootfn.org site, the landing page and the builds modal, deployed to Cloudflare Pages.
