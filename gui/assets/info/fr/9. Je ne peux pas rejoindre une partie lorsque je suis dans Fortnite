As explained in the "What is the Unreal Engine console?" section, the "Play" button doesn't work in many Fortnite versions.
Instead, you need to click the key assigned to the Unreal Engine console, by default F8 or the tilde(the button above tab), and type open 127.0.0.1

Comme expliqué dans la section "Qu'est-ce que la console Unreal Engine", le bouton "Jouer" ne fonctionne pas dans beaucoup de versions de Fortnite.
À la place, vous cevez appuyer sur la touche assignée à la console Unreal Engine qui, par défaut, est F8 (ou une touche non disponible sur un clavier français), et entrez 127.0.0.1